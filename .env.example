# Server-only values. Never prefix DATABASE_URL with VITE_ or expose it to browser code.
NODE_ENV=development
DATABASE_URL=postgresql://bibleon_app:change-me@localhost:5432/bibleon
DATABASE_POOL_MAX=10
LANGGRAPH_CHECKPOINT_SCHEMA=bibleon_langgraph
